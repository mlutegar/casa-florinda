# Deploy desativado — o site agora é servido pelo Vercel.
# O Vercel detecta automaticamente pushes para o branch main
# e faz o deploy sem precisar de GitHub Actions.
#
# Para reativar este workflow, descomente o conteúdo abaixo.
